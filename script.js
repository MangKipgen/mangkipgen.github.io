document.addEventListener('DOMContentLoaded', () => {
    const retroButton = document.getElementById('myRetroButton');

    retroButton.addEventListener('click', () => {
        alert('Hello from the early 2000s web!');
        // You'll add your subtle modern interactions here later!
    });
});